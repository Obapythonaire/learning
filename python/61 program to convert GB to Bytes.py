# program to convert GB to Bytes

# Gigabyte = int(input(("Enter the Gigabyte: ")))
# to_bytes = Gigabyte/(1024*1024*1024)

# print(f"{Gigabyte}GB is {to_bytes} bytes")

# program to convert Bytes to GB
byte = int(input(("Enter the byte: ")))
giga_bytes = byte*(1024*1024*1024)

print(f"{byte}GB is {giga_bytes} bytes")