# program to convert radians to degrees

from math import degrees, radians


# rad = float(input("Enter th number in rad: "))

# deg = degrees(rad)

# print(f"{rad}rad in degrees is {deg} degrees")

# ASS
# program to convert to degrees from radian

deg = float(input("Enter the number in deg: "))

rad = radians(deg)

print(f"{deg} degrees in radians is {rad} radians")
